import { Router } from "express";
const employeeRoutes = Router();

// employeeRoutes.get("/", getRoles);
// employeeRoutes.post("/create", createRole);
// employeeRoutes.delete("/:id", deleteRole);
// employeeRoutes.patch("/:id", updateRole);
// employeeRoutes.get("/:id", getEmployeeById);

export default employeeRoutes;
